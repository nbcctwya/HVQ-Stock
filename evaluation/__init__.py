"""Formal evaluation layer for HVQ-Stock (Baseline Results Protocol v1.0).

This package is independent of the research pipeline (Phase1/2/3 and the
root ``backtest_qlib.py``). It re-computes prediction metrics, re-runs the
unified Qlib backtest, aggregates seeds and builds ensembles from completed,
accepted formal artifacts only.
"""
